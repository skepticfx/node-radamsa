language: node_js
node_js:
  - 4.2.4
branches:
  only:
    - master
before_install:
  curl http://haltp.org/download/radamsa-0.4.tar.gz \
  | tar -zxvf - && cd radamsa-0.4 && make && sudo make install && cd ..
script:
  npm test
